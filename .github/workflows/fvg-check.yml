name: FVG Check

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger FVG check on Vercel
        run: |
          curl -sS --max-time 30 "https://aicoin-lark-webhook.vercel.app/api/cron/fvg-check"
